<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Lato&display=swap" rel="stylesheet">
<style>
* {
	margin: 0;
	padding: 0;
	border: 0;
	font-size: 100%;
	font: inherit;
	vertical-align: baseline;
  box-sizing: border-box;
}

body {
  font-size: 1.25em;
  font-family: 'Lato', sans-serif;
  background-color: #121212;
  display: flex;
  justify-content: center;
}

h1 {
  font-size: 2em;
}

svg {
  fill: white;
}

svg:hover {
  fill: #ffffffb3;
}

.container {
  display: grid;
  grid-template-columns: 1fr 5fr 1fr;
  grid-template-areas: 
    "title title github"
    "form form cancel"
    "list list list";
    row-gap: .5em;
  width: 60%;
  margin-top: 1em;
  color: white;
}

#title {
  grid-area: title;
}

#github {
  grid-area: github;
  justify-self: end;
  align-self: center;
}

form {
  grid-area: form;
  display: flex;
}

#cancel {
  grid-area: cancel;
  justify-self: end;
  align-self: center;
  visibility: hidden;
}

label {
  font-size: 1.25em;
}

input {
  width: 100%;
  border: none;
  outline: none;
  background-color: transparent;
  color: white;
  padding: 0 .5em;
  font-size: 1.25em;
  flex-grow: 1;
}

#autocomplete-list {
  grid-area: list;
  display: flex;
  flex-direction: column;
  height: 70vh;
  overflow-y: scroll;
  overflow-x: hidden;
  -ms-overflow-style: none;
  scrollbar-width: none;
}

#autocomplete-list::-webkit-scrollbar {
  display: none;
}

#autocomplete-list div {
  width: 100%;
  padding: .5em .25em;
  cursor: pointer;
  background-color: transparent;
  border: none;
  border-bottom: 1px solid #ffffff1a;
}

#autocomplete-items div:hover {
  background-color: rgba(251, 255, 30, 0.2);
}

.autocomplete-active {
  background-color: rgba(251, 255, 30, 0.2) !important;
}

#modal {
  display: none;
  position: fixed;
  z-index: 1;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  overflow: auto;
  background-color: rgba(0,0,0,0.3);
  color: white;
  justify-content: center;
}

#modal-content {
  width: 60%;
  height: max-content;
  margin-top: 2em;
  padding: 1em 1em;
  display: flex;
  flex-direction: column;
  background-color: #1a1a1a;
}

.modal-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: .5em;
}

.modal-button {
  padding: 1em 1em;
  text-decoration: none;
  color: white;
  background-color: #ffffff0d;
}

.modal-button:hover {
  background-color: #ffffff1a;
}

a {
  text-decoration: none;
}

@media only screen and (max-width: 800px) {
  .container {
    width: 90%;
  }
  #modal-content {
    width: 85%;
  }
}
</style>